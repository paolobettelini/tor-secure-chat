package com.example.convertitore.datepicker;

import android.app.DatePickerDialog;
import android.app.Dialog;
import android.content.Context;
import android.os.Bundle;

import androidx.fragment.app.DialogFragment;

import java.util.Calendar;

public class DatePickerFragment extends DialogFragment {

    //public Dialog onCreateDialog(Bundle savedInstanceState) {
    //    Calendar mCalendar = Calendar.getInstance();
    //    int year = mCalendar.get(Calendar.YEAR);
    //    int month = mCalendar.get(Calendar.MONTH);
    //    int dayOfMonth = mCalendar.get(Calendar.DAY_OF_MONTH);
    //    return new DatePickerDialog(getActivity(), (DatePickerDialog.OnDateSetListener)
    //            getActivity(), year, month, dayOfMonth);
    //}
//
    //DatePickerDialog.OnDateSetListener listener;
    //@Override
    //public void onAttach(Context context) {super.onAttach(context);
    //    try {
    //        listener = (DatePickerDialog.OnDateSetListener) context;
    //    } catch (ClassCastException e) {
    //        throw new ClassCastException(getActivity().toString()+ " must implement MyDialogListener");
    //    }
    //}

}